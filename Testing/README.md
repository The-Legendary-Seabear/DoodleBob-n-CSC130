# Unit tests

`unit-tests.js` uses Node's built-in test runner and does not require an npm package.

From the project root, run:

```text
node --test Testing/unit-tests.js
```

The suite covers song creation, library persistence, case-insensitive removal, empty-library rendering, playlist loading, and playlist song rendering.
